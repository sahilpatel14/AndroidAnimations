<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ninthsemester.androidanimations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SampleAnimActivity" />
        <activity android:name=".valueAnimator.ValueAnimatorActivity" />
        <activity android:name=".objectAnimator.ObjectAnimatorActivity" />
        <activity android:name=".valueAnimator.BouncingBallsActivity" />
        <activity android:name=".viewGroupsLayoutChanges.LayoutAnimationDefaultActivity">

        </activity>
        <activity android:name=".viewGroupsLayoutChanges.LayoutAnimationHideShowActivity">

        </activity>
    </application>

</manifest>